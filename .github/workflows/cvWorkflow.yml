name: Cv Workflow

on: [push,pull_request]

jobs:
  release:
   runs-on: ubuntu-20.04
   steps: 
    - name: Install dependancies
      run: echo 'dependancies are installed!'
    - name: End message
      run: echo 'All done!'
   
  
   
